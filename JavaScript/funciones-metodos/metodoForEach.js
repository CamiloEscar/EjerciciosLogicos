// metodo .forEach() ejecuta una funcion para cada elemento del arreglo, en este ejemplo estamos agregando una palabra (Color) para cada elemento,para que se imprima en la consola asi:

// Color rojo,
// Color azul, etc.""

const colores = ["rojo", "azul", "verde", "negro"];

colores.forEach((color) => console.log(`Color ${color}`));
